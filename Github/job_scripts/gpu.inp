#PBS -q accq
#PBS -l select=1:ncpus=1:ngpus=1:mem=8gb
#PBS -j oe
#PBS -m abe
#PBS -M fe1855@wayne.edu

echo Running on host `hostname`
echo Time is `date`

cd RUN_DIR
echo Directory is `pwd`

module swap gnu7/7.3.0 intel/2018
module load cuda/10.0

# Run job

COMMAND

